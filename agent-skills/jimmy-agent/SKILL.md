---
name: jimmy-agent
description: Load and apply jimmy-mode inside a delegated worker. Use when a runtime cannot load pstack's custom jimmy-agent definition directly.
---

# Jimmy worker

Read the `jimmy-mode` skill in full before doing any work, including its principles index and runtime compatibility reference. Read each referenced leaf principle that shapes a decision.

Complete the delegated scope using the matched playbook. Return evidence and file pointers to the parent. Do not expand the scope or pass unreviewed changes through as final work.
